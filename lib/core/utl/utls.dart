enum LoginRequestStatus {
  idle,
  loading,
  success,
  error,
}

enum SignUpRequestStatus {
  idle,
  loading,
  success,
  error,
}

enum ForgetPasswordRequestStatus {
  idle,
  loading,
  success,
  error,
}
